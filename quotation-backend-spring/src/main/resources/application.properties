# Application Configuration
spring.application.name=quotation-backend

# Server Configuration
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# Database Configuration
# WARNING: Update these credentials before deploying to production
# Consider using environment variables or Spring Cloud Config for sensitive data

#spring.datasource.url=jdbc:postgresql://localhost:5432/qoutdb
#spring.datasource.username=postgres
#spring.datasource.password=0000
#spring.datasource.driver-class-name=org.postgresql.Driver

###############################################

spring.datasource.url=jdbc:postgresql://dpg-d739otm3jp1c738tcb90-a.singapore-postgres.render.com:5432/mydb_bacp
spring.datasource.username=myuser
spring.datasource.password=JXgINW5za0LebW09oUV664Gr3I4nbqoe

spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
# ? VERY IMPORTANT (Render SSL)
spring.datasource.hikari.data-source-properties.sslmode=require
#####################################################################
# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# SECURITY WARNING: Change this secret key before deploying to production!
# The secret should be at least 256 bits (32 characters) and randomly generated
# Recommended: Use environment variable JWT_SECRET instead of hardcoding
jwt.secret=your_super_secret_jwt_key_change_this_in_production_minimum_256_bits
jwt.expiration=604800000

# CORS Configuration
cors.allowed.origins=http://localhost:5173

# Logging
logging.level.com.quotation=DEBUG
logging.level.org.springframework.security=DEBUG
